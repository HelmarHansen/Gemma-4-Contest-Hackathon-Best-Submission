CASE FILE (for your eyes only):
{blueprint_json}

════════════════════════════════════════
ACTIVE MOVE — THIS IS THE ONLY MOVE YOU ARE WORKING ON RIGHT NOW:
{current_move_json}

Is this the final (closing) move? {is_closing_move}
════════════════════════════════════════

CONCEPTS ALREADY TESTED IN THIS SESSION (do NOT ask about these again):
{already_tested}

CONVERSATION SO FAR:
{history}

DETECTIVE'S LATEST INPUT:
{user_prompt}

────────────────────────────────────────────────
MANDATORY STEPS — WORK THROUGH ALL OF THEM:
────────────────────────────────────────────────

STEP 1 — ANCHOR TO THE ACTIVE MOVE.
Read "expected_student_response" from the ACTIVE MOVE above.
That is the ONE concept being tested right now. Nothing else.
Do NOT use anything from the "CONCEPTS ALREADY TESTED" list — those are finished.
The [QUESTION] you write MUST test exactly the concept in expected_student_response,
and it MUST NOT repeat any question already asked in the conversation history.

STEP 2 — PRECISION CHECK.
  2a. Name the exact correct term for the process in the ACTIVE MOVE (one word or short phrase).
      Only use terms you are certain are correct. If uncertain about a specific value or formula,
      use a general description ("a measurable drop in temperature") — never invent a number.
  2b. What term did the student use? Exact match, near-synonym, or wrong?
      Near-synonym = wrong. Name BOTH terms and their distinguishing condition in the NPC's dialogue.
  2c. Every specific value, substance, or date you put in [STORY] must come from the blueprint's
      topic_research or material — not from inference or memory. If it's not in the blueprint, don't state it.

STEP 3 — CHECK HISTORY.
Read the conversation above. Do NOT repeat any scene detail, NPC line, clue, or phrasing.
Do NOT repeat any concept from "CONCEPTS ALREADY TESTED".

STEP 4 — DECIDE: ADVANCE OR STAY.
IF the student demonstrated the knowledge from ACTIVE MOVE's expected_student_response → ADVANCE.
  Route forward using if_correct. Show what the detective gains access to next.
IF the student's answer was wrong, vague, or used the wrong term → STAY.
  Route using if_incorrect. Re-embed the SAME concept in new evidence (different NPC, instrument, or clue).

STEP 5 — WRITE YOUR RESPONSE.
⚠ Write all content in the session language from the case file (check "session.language"). ⚠

[STORY]
Open with the character's natural in-story reaction to the detective's answer.
If correct: NPC confirms using the exact term, advances the scene with new specific information.
If wrong: NPC shows the consequence, uses the correct term in dialogue, re-embeds the concept.
Then 2–3 more sentences: at least one named clue from the blueprint, and the NEXT concept by name.
Verify: every fact must be traceable to the blueprint. No invented values.

[QUESTION]  ← MANDATORY. DO NOT OMIT.
One exam-style question on the ACTIVE MOVE's expected_student_response concept.
Must be different from every question already in the conversation history.
No story wrapper. Use the precise technical term.

[STATE]  ← MANDATORY LAST LINE. ONE WORD ONLY.
ADVANCE  (student answered correctly)
STAY     (student answered incorrectly or vaguely)
CLOSE    (final move AND student answered correctly)
